import { PublicKey } from '@solana/web3.js';
import { Dare } from '@/types';

export const getMockDares = (): Dare[] => {
  return [];
};

// Export the mock PublicKeys for matching in API routes
export const MOCK_DARE_IDS: string[] = [];
